# ==============================================
# TravelMind Environment Configuration
# ==============================================

# ---- AI Provider Configuration ----
# AI providers are now configured per user in the Settings page
# Supported providers: Claude (Anthropic), OpenAI, Google Gemini
# Each user enters their own API key which is encrypted and stored securely

# ---- Backend Configuration (FastAPI) ----
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
BACKEND_RELOAD=true
# Set to false in production

# ---- Frontend Configuration (React + Vite) ----
VITE_API_URL=http://localhost:8000
VITE_APP_NAME=TravelMind
VITE_APP_VERSION=1.0.0
FRONTEND_URL=http://localhost:5173

# ---- Database Configuration ----
DATABASE_URL=sqlite:///./data/travelmind.db
# For PostgreSQL use:
# DATABASE_URL=postgresql://user:password@localhost:5432/travelmind

# ---- Security & Authentication ----
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRES_IN=7d
SECRET_KEY=your-secret-key-here-change-in-production

# ---- CORS Settings ----
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
CORS_ALLOW_CREDENTIALS=true

# ---- Map Provider ----
# OpenStreetMap (default, no key needed) or Mapbox
MAP_PROVIDER=openstreetmap
# For Mapbox:
# MAP_PROVIDER=mapbox
MAPBOX_ACCESS_TOKEN=your-mapbox-token-optional

# ---- Storage & Upload ----
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760
MAX_UPLOAD_SIZE_MB=10
ALLOWED_EXTENSIONS=jpg,jpeg,png,gif,webp,pdf

# ---- Feature Flags ----
ENABLE_AI_FEATURES=true
ENABLE_COLLABORATION=true
ENABLE_OFFLINE_MODE=true

# ---- Speech-to-Text (Whisper API) ----
# Optional: For voice-to-text diary entries
# Get your API key from: https://platform.openai.com/api-keys
# Cost: ~$0.006 per minute of audio
OPENAI_API_KEY=your-openai-api-key-here-optional

# ---- Demo Mode ----
# Enable demo mode to allow unauthenticated access (for testing/demos only!)
# Set to false in production for security
ENABLE_DEMO_MODE=false
# Demo user password (required if demo mode is enabled)
# DEMO_PASSWORD=your-secure-demo-password

# ---- Logging ----
LOG_LEVEL=INFO
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

# ---- Production Settings ----
# Uncomment for production:
# NODE_ENV=production
# BACKEND_RELOAD=false
# LOG_LEVEL=WARNING
# SECRET_KEY=<generate-strong-random-key>
